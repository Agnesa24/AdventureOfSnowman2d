using UnityEngine;

public class CannonManager : MonoBehaviour
{

    ////this class is for when you want to add a manager that does the swtching between the 2 cannons 
    //public shootBullet gun1;
    //public shootBullet gun2;

    //// Start is called once before the first execution of Update after the MonoBehaviour is created
    //void Start()
    //{
    //    // Start with gun1 active and the other one inactive
    //    gun1.SetActive(true);
    //    gun2.SetActive(false);

    //    InvokeRepeating(nameof(SwitchGuns), 20f, 20f);// after the first 20 seconds, the SwitchGuns method will be called every 20 seconds to toggle the active state of the two guns
    //}

    //// Update is called once per frame
    //void Update()
    //{
        
    //}
    //void SwitchGuns()
    //{//when one is on the other one is off 
    //    if (gun1.IsActive())
    //    {
    //        gun1.SetActive(false);
    //        gun2.SetActive(true);
    //    }
    //    else
    //    {
    //        gun1.SetActive(true);
    //        gun2.SetActive(false);
    //    }
    //}
}
